# Figures

Curated, committed result figures referenced from the top-level `README.md`.

These are copied here by hand from `../outputs/` after running the notebook (see
"Publish the headline figures" in the main README). Unlike `outputs/` — which holds the
full machine-generated dump and is git-ignored — this folder is tracked, so only the few
figures worth showcasing live here.

Expected files (referenced by the README):
- `distribution_esg_tilt_score.png`
- `distribution_esg_tilt_involvement.png`
- `horse_race_all_models.png`
